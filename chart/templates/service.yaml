apiVersion: v1
kind: Service
metadata:
  name: {{ include "subtitle-player.fullname" . }}
  labels:
    {{- include "subtitle-player.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "subtitle-player.selectorLabels" . | nindent 4 }}
